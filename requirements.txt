# Core (CPU, numpy-only: data loading, evaluation sets, metrics, Sarkar construction, audits)
numpy>=1.24
pandas>=2.0
mpmath>=1.3
# Training (GPU strongly recommended; every archived run used an A100 on Colab Pro+)
torch>=2.1
geoopt==0.5.1
# Notebooks / figures
jupyter
matplotlib
tqdm
requests
# Tests
pytest
